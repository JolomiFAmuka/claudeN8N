# This repo has core.autocrlf=true locally. Without the rule below, init-data.sh
# would be checked out with CRLF endings, and the Postgres container would fail
# to run it ("bad interpreter: /bin/bash^M"). Shell scripts must stay LF.
* text=auto
*.sh text eol=lf
